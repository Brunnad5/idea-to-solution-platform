# ============================================
# Umgebungsvariablen für Idea2Solution Platform
# ============================================
# Kopiere diese Datei nach .env.local und fülle die Werte aus.
# .env.local wird NICHT ins Git-Repository eingecheckt (ist in .gitignore).

# --------------------------------------------
# DATAVERSE KONFIGURATION
# --------------------------------------------

# Die URL deiner Dataverse-Umgebung (Power Platform Environment)
DATAVERSE_URL=https://scepdevstud6.crm17.dynamics.com

# Manuell geholter Access Token für Dataverse
# ACHTUNG: Token läuft nach ca. 1 Stunde ab!
# Anleitung zum Holen des Tokens: siehe docs/token-anleitung.md
DATAVERSE_ACCESS_TOKEN=

# (Optional) Business Process Flow Process ID
# Wird verwendet um die Stages dynamisch aus Dataverse zu laden.
# Wenn nicht gesetzt, werden Standard-Stages verwendet.
# BPF_PROCESS_ID=

# --------------------------------------------
# MOCK-USER KONFIGURATION (für Prototyp)
# --------------------------------------------
# Da wir keine echte Microsoft-Authentifizierung haben,
# simulieren wir den eingeloggten User mit diesen Werten.

# Dein Name (wird als "Eingereicht von" angezeigt)
MOCK_USER_NAME=Adrian Brunner

# Deine E-Mail (für Berechtigungsprüfung "eigene Ideen bearbeiten")
MOCK_USER_EMAIL=adrian.brunner@sgsw.ch
